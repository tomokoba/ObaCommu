<div class="main">
  <div class="card devise-card">
    <div class="form-wrap">
      <div class="form-group text-center mb-4">
        <h2 class="mx-auto mb-4">ObaCommu</h2>
        <p>会員登録時に利用した方法で、ログインください。</p>
      </div>
      <div>
        <%= form_with model: @user, url: user_session_path, local: true do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="form-group mb-4">
            <%= f.label "メールアドレスの入力", class: "font-weight-bold mb-0" %><br />
            <div class="mb-2">ご利用されているメールアドレスをご入力ください。</div>
            <%= f.email_field :email, autofocus: true, placeholder: "例) example@example.com", class: "form-control" %>
          </div>

          <div class="form-group mb-4">
            <%= f.label "パスワードの入力", class: "font-weight-bold mb-0" %><br />
            <div class="mb-2">半角英数字でご入力ください。</div>
            <%= f.password_field :password, autocomplete: "off", placeholder: "例) AbcDe0123fg", class: "form-control" %>
          </div>

          <div class="actions">
            <%= f.submit "ログインする", class: "btn btn-green w-100" %>
          </div>
        <% end %>
      </div>
      <div class="mt-4">
        はじめてご利用の方ですか？ <%= link_to "登録する", new_user_registration_path %>
      </div>
      <div>
        パスワードをお忘れですか？ <%= link_to "変更する", new_user_password_path %>
　　　</div>
    </div>
  </div>
</div>
