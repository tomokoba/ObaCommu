<div class="main">
  <div class="card devise-card">
    <div class="form-wrap">
      <div class="form-group mb-4">
        <h3 class="mx-auto text-center">パスワード変更</h3>
        <p class="mt-3">新しいパスワードを以下のフォームにご入力ください。</p>
      </div>
      <div>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>
          <%= f.hidden_field :reset_password_token %>

          <div class="field mb-3">
            <%= f.label :password, "新しいパスワード" %>
            <% if @minimum_password_length %>
              <em>(<%= @minimum_password_length %>文字以上*)</em><br />
            <% end %>
            <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: 'form-control' %>
          </div>

          <div class="field mb-3">
            <%= f.label :password_confirmation, "新しいパスワード（再度ご入力ください）" %><br />
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control' %>
          </div>

          <div class="actions">
            <%= f.submit "パスワードを変更する", class: "btn btn-green w-100" %>
          </div>
        <% end %>
      </div>
      <div class="mt-4 mb-1">
        はじめてご利用の方ですか？ <%= link_to "会員登録する", new_user_registration_path %>
      </div>
      <div>
        すでに会員登録済ですか？ <%= link_to "ログインする", new_user_session_path %>
　　　</div>
    </div>
  </div>
</div>