<div class="topic">
  <div class="container">
    <h1 class="topic-font my-4">お問い合わせ内容確認</h1>
  </div>
</div>

<div class="main">
  <div class="card devise-card">
    <div class="form-wrap">
      <div class="form-group mb-0">
        <h3 class="mx-auto text-center">お問い合わせ内容</h3>
        <p class="text-center mt-3">
          内容はこちらでお間違いないでしょうか？<br />
          よろしければ【送信する】ボタンを押してください。<br />
          48時間以内にメールにて回答いたします。
        </p>
        <table class="table table-bordered table-condensed">
          <tbody>
            <tr>
              <td>お名前</td>
              <td class="text-left"><%= @contact.name %></td>
            </tr>
            <tr>
              <td>メールアドレス</td>
              <td><%= @contact.email %></td>
            </tr>
            <tr>
              <td>お問い合わせ内容</td>
              <td style="white-space: pre-wrap;"><%= @contact.message %></td>
            </tr>
          </tbody>
        </table>

        <div class="text-center mt-4">
        <%= form_with model: @contact, local: true do |f| %>
          <%= f.hidden_field :name %>
          <%= f.hidden_field :email %>
          <%= f.hidden_field :message %>
          <%= f.submit '送信する', class: "btn btn-primary btn-shadow w-100" %>
        <% end %>
        <%= form_with model: @contact, url: back_path, local: true do |f| %>
          <%= f.hidden_field :name %>
          <%= f.hidden_field :email %>
          <%= f.hidden_field :message %>
          <div class="mt-3">内容変更はこちら→ <%= f.submit '入力画面に戻る', class: "btn btn-link p-0" %></div>
        <% end %>
        </div>
      </div>
　  </div>
  </div>
</div>