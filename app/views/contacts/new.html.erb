<div class="topic">
  <div class="container">
    <h1 class="topic-font my-4">お問い合わせ</h1>
  </div>
</div>

<div class="main">
  <div class="card devise-card">
    <div class="form-wrap">
      <div class="form-group mb-4">
        <h3 class="mx-auto text-center">お問い合わせフォーム</h3>
        <p class="mt-3">お問い合わせ内容を以下のフォームにご入力ください。</p>
        <p>
          *回答は全てメールで返信させていただきます。<br />
          <span class="text-danger">ご登録のメールアドレスが正しくない場合</span>は、返信のメールが届きません。<br />
          問い合わせる前に<%= link_to "会員情報変更", edit_user_path(current_user) %>からご登録メールアドレスの変更をお願いいたします。
        </p>
      </div>
      <%= form_with model: @contact, url: confirm_path, local:true do |f| %>
        <div class="form-group">
          <%= f.label :name, 'お名前*' %>
          <%= f.text_field :name, autofocus: true, class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.label :email, 'メールアドレス*' %><br />
          <%= f.email_field :email, autofocus: true, placeholder: "例) example@example.com", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :message, 'お問い合わせ内容*' %>
          <%= f.text_area :message, size: '10x10', placeholder: "例）ログインできないのですが、どのようにしたらよいのでしょうか？", class: 'form-control' %>
        </div>

        <div>
          <%= f.submit '入力内容確認', class: "btn btn-primary btn-shadow w-100" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
